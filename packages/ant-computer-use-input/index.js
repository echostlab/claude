async function noop() {}

export const isSupported = false
export const moveMouse = noop
export const click = noop
export const key = noop
export const keys = noop
export const scroll = noop
export const drag = noop
